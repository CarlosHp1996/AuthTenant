# UserDto.cs - DTO de Usuário

## 📋 Visão Geral
DTO (Data Transfer Object) que representa informações de usuário de forma segura para respostas de API. Exclui informações sensíveis como senhas e implementa suporte à arquitetura multi-tenant com isolamento de dados.

## 🏗️ Arquitetura e Estrutura

### Classe Selada
```csharp
public sealed class UserDto
```
- **Sealed**: Previne herança para manter controle total sobre a estrutura
- **Thread-Safe**: Design imutável após construção
- **Security-First**: Campos sensíveis são excluídos por design

## 📋 Propriedades Principais

### Identificação do Usuário
```csharp
[Required]
[JsonPropertyName("id")]
public string Id { get; set; } = string.Empty;
```
- **Tipo**: GUID string representation
- **Requerido**: Campo obrigatório para identificação única
- **JSON**: Serializado como "id" em lowercase

### Informações de Contato
```csharp
[Required]
[EmailAddress]
[JsonPropertyName("email")]
public string Email { get; set; } = string.Empty;
```
- **Validação**: Email válido obrigatório
- **Único**: Deve ser único dentro do tenant
- **Autenticação**: Usado como identificador principal

### Dados Pessoais
```csharp
[Required]
[StringLength(50, MinimumLength = 1)]
[JsonPropertyName("firstName")]
public string FirstName { get; set; } = string.Empty;

[Required]
[StringLength(50, MinimumLength = 1)]
[JsonPropertyName("lastName")]
public string LastName { get; set; } = string.Empty;
```
- **Limitações**: 1-50 caracteres cada
- **Personalização**: Usado para interface personalizada
- **Display**: Combinado para formar nome completo

### Multi-Tenancy
```csharp
[Required]
[JsonPropertyName("tenantId")]
public string TenantId { get; set; } = string.Empty;
```
- **Isolamento**: Garante isolamento de dados entre tenants
- **Segurança**: Crítico para controle de acesso
- **Validação**: Verificado em todas as operações

## 🔒 Propriedades de Segurança

### Status da Conta
```csharp
[JsonPropertyName("isActive")]
public bool IsActive { get; set; } = true;
```
- **Controle de Acesso**: Desativa conta sem deletar dados
- **Auditoria**: Rastreia status do usuário
- **Default**: Usuários são ativos por padrão

### Verificação de Email
```csharp
[JsonPropertyName("emailVerified")]
public bool EmailVerified { get; set; } = false;
```
- **Segurança**: Confirma validade do email
- **Processo**: Verificado via token de confirmação
- **Default**: Não verificado inicialmente

### Informações de Sessão
```csharp
[JsonPropertyName("lastLoginAt")]
public DateTime? LastLoginAt { get; set; }
```
- **Auditoria**: Rastreia atividade do usuário
- **Nullable**: Null para usuários que nunca logaram
- **UTC**: Sempre em formato UTC

## 🎯 Propriedades Computadas

### Nome Completo
```csharp
[JsonPropertyName("fullName")]
public string FullName => CreateFullName(FirstName, LastName);
```
- **Read-Only**: Propriedade calculada dinamicamente
- **Smart Formatting**: Lida com nomes vazios graciosamente
- **Consistent**: Sempre formatado consistentemente

### Nome de Exibição Efetivo
```csharp
[JsonPropertyName("effectiveDisplayName")]
public string EffectiveDisplayName => !string.IsNullOrWhiteSpace(DisplayName) ? DisplayName : FullName;
```
- **Fallback**: DisplayName personalizado ou FullName
- **Flexibilidade**: Permite nomes profissionais/apelidos
- **UI**: Usado em interfaces de usuário

### Indicadores de Status
```csharp
[JsonPropertyName("isNewUser")]
public bool IsNewUser => LastLoginAt == null;

[JsonPropertyName("daysSinceCreation")]
public int DaysSinceCreation => (DateTime.UtcNow - CreatedAt).Days;
```
- **Business Logic**: Indicadores úteis para UI/UX
- **Analytics**: Dados para análise de comportamento
- **Lifecycle**: Gestão do ciclo de vida do usuário

## 🛡️ Autorização e Roles

### Sistema de Roles
```csharp
[JsonPropertyName("roles")]
public IReadOnlyList<string> Roles { get; set; } = new List<string>();
```
- **Read-Only Collection**: Previne modificação acidental
- **Tenant-Scoped**: Roles específicas por tenant
- **Authorization**: Usado para controle de acesso

## 🔧 Métodos Auxiliares

### Criação de Nome Completo
```csharp
private static string CreateFullName(string? firstName, string? lastName)
{
    return (first, last) switch
    {
        ("", "") => "Unknown User",
        (var f, "") => f,
        ("", var l) => l,
        (var f, var l) => $"{f} {l}"
    };
}
```
- **Pattern Matching**: Usa switch expressions modernas
- **Null Safe**: Lida com todos os cenários de entrada
- **Fallback**: "Unknown User" para casos extremos

### Validação de Email
```csharp
private static bool IsValidEmail(string email)
{
    try
    {
        var addr = new System.Net.Mail.MailAddress(email);
        return addr.Address == email;
    }
    catch
    {
        return false;
    }
}
```
- **Framework Native**: Usa validação do .NET
- **Exception Safe**: Não lança exceções
- **Strict**: Validação rigorosa de formato

## ✅ Métodos de Validação

### Validação Completa
```csharp
public bool IsValid()
{
    return !string.IsNullOrWhiteSpace(Id) &&
           !string.IsNullOrWhiteSpace(Email) &&
           !string.IsNullOrWhiteSpace(FirstName) &&
           !string.IsNullOrWhiteSpace(LastName) &&
           !string.IsNullOrWhiteSpace(TenantId) &&
           IsValidEmail(Email);
}
```
- **Comprehensive**: Verifica todos os campos obrigatórios
- **Business Rules**: Aplica regras de negócio específicas
- **Consistency**: Garante integridade dos dados

## 🔄 Serialização JSON

### Naming Strategy
```csharp
[JsonPropertyName("propertyName")]
```
- **camelCase**: Consistência com padrões web
- **Explicit**: Nomes explícitos para controle total
- **API Friendly**: Formato esperado por frontends

### Propriedades Ignoradas
```csharp
[JsonIgnore]
public string InternalProperty { get; set; }
```
- **Internal Use**: Dados não expostos via API
- **Security**: Previne vazamento de informações
- **Clean API**: Interface limpa para consumidores

## 🎯 Padrões de Uso

### Resposta de Autenticação
```csharp
var authResponse = new AuthResponseDto
{
    User = userDto,
    Token = "...",
    // ...
};
```

### Listagem de Usuários
```csharp
var users = await userService.GetUsersAsync();
var userDtos = mapper.Map<List<UserDto>>(users);
```

### Profile do Usuário
```csharp
var currentUser = userDto.EffectiveDisplayName;
var isNew = userDto.IsNewUser;
```

## 🔒 Considerações de Segurança

### Dados Excluídos
- ❌ **PasswordHash**: Nunca expostos
- ❌ **SecurityStamp**: Informações de segurança internas
- ❌ **Salt**: Dados de criptografia
- ❌ **Tokens**: Tokens de acesso/refresh

### Dados Incluídos Seguros
- ✅ **Email**: Identificação necessária
- ✅ **Roles**: Para autorização frontend
- ✅ **Status**: Para controle de UI
- ✅ **Metadata**: Informações não sensíveis

## 📊 Performance

### Propriedades Calculadas
- **Lazy Evaluation**: Calculadas apenas quando acessadas
- **No Caching**: Sempre atualizadas
- **Lightweight**: Operações simples

### Serialização
- **Efficient**: Apenas dados necessários
- **Compact**: JSON otimizado
- **Fast**: Serialização rápida

## 🧪 Testabilidade

### Factory Methods
```csharp
public static UserDto CreateForTesting(string email, string tenantId)
{
    return new UserDto
    {
        Id = Guid.NewGuid().ToString(),
        Email = email,
        TenantId = tenantId,
        // ...
    };
}
```

### Validation Testing
```csharp
[Test]
public void UserDto_WithValidData_ShouldBeValid()
{
    var userDto = CreateValidUserDto();
    Assert.True(userDto.IsValid());
}
```

## 🔮 Extensibilidade

### Novos Campos
- **Backward Compatible**: Adicionar campos opcionais
- **Versioning**: Considerar versionamento de API
- **Migration**: Planos para migração de dados

### Custom Properties
- **DisplayName**: Já preparado para personalização
- **Metadata**: Campos adicionais conforme necessário
- **Preferences**: Configurações do usuário

## 🎯 Boas Práticas Implementadas

### Data Transfer Object Pattern
- **Separation of Concerns**: Separado de entidades de domínio
- **API Contract**: Define contrato claro da API
- **Validation**: Validação específica para transferência

### Security by Design
- **Least Privilege**: Apenas dados necessários expostos
- **Input Validation**: Validação rigorosa de entrada
- **Output Sanitization**: Dados limpos na saída

### Multi-Tenant Architecture
- **Tenant Isolation**: TenantId sempre presente
- **Data Segregation**: Dados isolados por tenant
- **Security Context**: Contexto de segurança preservado

---

## 📝 Notas de Implementação

- ✅ Todas as propriedades são validadas adequadamente
- ✅ Serialização JSON otimizada para APIs web
- ✅ Métodos auxiliares são testáveis e seguros
- ✅ Multi-tenancy suportado nativamente
- ✅ Propriedades computadas para melhor UX
- ✅ Validação de email robusta implementada
- ✅ Segurança garantida através de design
