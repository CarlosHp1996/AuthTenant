# CreateProductDto.cs - Documentação Técnica

## 📋 Visão Geral
O `CreateProductDto` é um Data Transfer Object especializado para criação de produtos, implementando validação robusta, sanitização de dados e regras de negócio específicas para um ambiente multi-tenant. Garante integridade e consistência desde o momento da criação.

## 🏗️ Arquitetura e Design

### Padrões Implementados
- **Data Transfer Object (DTO)**: Transferência segura para operações de criação
- **Command Pattern**: Representa comando de criação de produto
- **Validation Pattern**: Validação multicamada e business rules
- **Builder Pattern**: Factory methods para testing e setup
- **Value Object**: Propriedades computadas para validação

### Princípios SOLID
- **SRP**: Focado exclusivamente em criação de produtos
- **OCP**: Extensível via metadata sem modificar código core
- **LSP**: Compatível com interface de criação
- **ISP**: Interface específica para criação
- **DIP**: Independente de persistence layer

## 🔧 Principais Funcionalidades

### Propriedades Obrigatórias
```csharp
[Required]
public string Name { get; set; }                // Nome do produto (2-200 chars)

[Required]
[Range(0.01, decimal.MaxValue)]
public decimal Price { get; set; }              // Preço inicial (> 0)
```

### Propriedades Opcionais
```csharp
public string? Description { get; set; }        // Descrição (max 2000 chars)
public string? SKU { get; set; }                // Código SKU único
public int StockQuantity { get; set; } = 0      // Estoque inicial
public bool IsActive { get; set; } = true       // Status inicial ativo
```

### Propriedades Avançadas
```csharp
public IList<string> Categories { get; set; }   // Categorias (max 10)
public IList<string> Tags { get; set; }         // Tags de busca (max 20)
public int MinimumStockLevel { get; set; } = 5  // Nível mínimo de estoque
public double? Weight { get; set; }             // Peso em gramas
public ProductDimensions? Dimensions { get; set; } // Dimensões físicas
public IDictionary<string, string>? Metadata    // Metadados personalizados
```

### Propriedades Computadas
```csharp
public string NormalizedName                    // Nome normalizado para busca
public string? NormalizedSKU                    // SKU normalizado
public bool HasSufficientInfo                   // Validação de informação suficiente
```

## ✅ Validação Avançada

### Validação de Nome
```csharp
private static bool IsValidName(string name)
{
    if (string.IsNullOrWhiteSpace(name))
        return false;

    // Verifica conteúdo significativo (não apenas espaços/caracteres especiais)
    var cleanName = Regex.Replace(name.Trim(), @"[^\w\s]", "");
    return cleanName.Length >= 2;
}
```

### Validação de SKU
```csharp
[RegularExpression(@"^[A-Za-z0-9\-_]+$")]
private static bool IsValidSKU(string? sku)
{
    if (string.IsNullOrWhiteSpace(sku))
        return true; // SKU é opcional

    return Regex.IsMatch(sku, @"^[A-Za-z0-9\-_]+$") && sku.Length <= 50;
}
```

### Validação de Coleções
```csharp
// Categorias: máximo 10, cada uma até 100 caracteres
private static bool IsValidCategories(IList<string> categories)
{
    if (categories == null || !categories.Any())
        return true;

    return categories.All(c => !string.IsNullOrWhiteSpace(c) && c.Length <= 100) &&
           categories.Count <= 10;
}

// Tags: máximo 20, cada uma até 50 caracteres
private static bool IsValidTags(IList<string> tags)
{
    if (tags == null || !tags.Any())
        return true;

    return tags.All(t => !string.IsNullOrWhiteSpace(t) && t.Length <= 50) &&
           tags.Count <= 20;
}
```

## 🔒 Sanitização e Segurança

### Limpeza Automática de Dados
```csharp
public void Sanitize()
{
    Name = Name?.Trim() ?? string.Empty;
    Description = string.IsNullOrWhiteSpace(Description) ? null : Description.Trim();
    SKU = string.IsNullOrWhiteSpace(SKU) ? null : SKU.Trim().ToUpperInvariant();
    
    // Limpa e normaliza categorias
    Categories = Categories?.Where(c => !string.IsNullOrWhiteSpace(c))
                          .Select(c => c.Trim())
                          .Distinct()
                          .ToList() ?? new List<string>();
    
    // Limpa e normaliza tags (lowercase para consistência)
    Tags = Tags?.Where(t => !string.IsNullOrWhiteSpace(t))
               .Select(t => t.Trim().ToLowerInvariant())
               .Distinct()
               .ToList() ?? new List<string>();

    // Garante que MinimumStockLevel não é maior que StockQuantity
    if (MinimumStockLevel > StockQuantity)
        MinimumStockLevel = Math.Max(0, StockQuantity - 1);
}
```

### Normalização para Busca
```csharp
private static string NormalizeName(string name)
{
    if (string.IsNullOrWhiteSpace(name))
        return string.Empty;

    return Regex.Replace(name.Trim(), @"\s+", " ").ToLowerInvariant();
}
```

## 📏 Suporte a Dimensões Físicas

### Classe ProductDimensions
```csharp
public sealed class ProductDimensions
{
    [Range(0, double.MaxValue)]
    public double Length { get; set; }           // Comprimento em cm
    
    [Range(0, double.MaxValue)]
    public double Width { get; set; }            // Largura em cm
    
    [Range(0, double.MaxValue)]
    public double Height { get; set; }           // Altura em cm
    
    public double Volume => Length * Width * Height; // Volume calculado
    
    public bool IsValid() => Length >= 0 && Width >= 0 && Height >= 0;
}
```

## 🔍 Validação de Regras de Negócio

### Método Principal de Validação
```csharp
public bool IsValid()
{
    return !string.IsNullOrWhiteSpace(Name) &&
           Name.Length >= 2 &&
           Name.Length <= 200 &&
           Price > 0 &&
           StockQuantity >= 0 &&
           MinimumStockLevel >= 0 &&
           (Description?.Length ?? 0) <= 2000 &&
           (SKU?.Length ?? 0) <= 50 &&
           (Weight ?? 0) >= 0 &&
           IsValidName(Name) &&
           IsValidSKU(SKU) &&
           IsValidCategories(Categories) &&
           IsValidTags(Tags);
}
```

### Coleta de Erros de Validação
```csharp
public IList<string> GetValidationErrors()
{
    var errors = new List<string>();

    if (string.IsNullOrWhiteSpace(Name))
        errors.Add("Product name is required");
    else if (Name.Length < 2)
        errors.Add("Product name must be at least 2 characters long");
    else if (Name.Length > 200)
        errors.Add("Product name cannot exceed 200 characters");

    if (Price <= 0)
        errors.Add("Price must be greater than 0");

    if (StockQuantity < 0)
        errors.Add("Stock quantity cannot be negative");

    // ... mais validações específicas

    return errors;
}
```

## 🧪 Testabilidade e Factory Methods

### Factory para Testes
```csharp
public static CreateProductDto CreateForTesting(string name, decimal price)
{
    return new CreateProductDto
    {
        Name = name,
        Price = price,
        StockQuantity = 100,
        IsActive = true
    };
}
```

### Exemplo de Uso em Testes
```csharp
[Test]
public void Should_Create_Valid_Product_Dto()
{
    // Arrange
    var dto = CreateProductDto.CreateForTesting("Test Product", 99.99m);
    
    // Act
    dto.Sanitize();
    var isValid = dto.IsValid();
    
    // Assert
    Assert.That(isValid, Is.True);
    Assert.That(dto.HasSufficientInfo, Is.True);
}
```

## 📊 Serialização JSON

### Exemplo de Payload
```json
{
  "name": "Premium Wireless Headphones",
  "description": "High-quality wireless headphones with active noise cancellation",
  "price": 299.99,
  "sku": "WH-PRE-001",
  "stockQuantity": 100,
  "isActive": true,
  "categories": ["Electronics", "Audio", "Headphones"],
  "tags": ["wireless", "premium", "noise-cancelling"],
  "minimumStockLevel": 10,
  "weight": 250.5,
  "dimensions": {
    "length": 20.5,
    "width": 18.0,
    "height": 8.5,
    "volume": 3135.75
  },
  "metadata": {
    "brand": "TechCorp",
    "warranty": "2-years",
    "color": "black"
  }
}
```

## 🔄 Integração com Command Handlers

### Uso em Create Command
```csharp
public class CreateProductCommand : IRequest<Result<Guid>>
{
    public CreateProductDto ProductData { get; set; }
}

public class CreateProductHandler : IRequestHandler<CreateProductCommand, Result<Guid>>
{
    public async Task<Result<Guid>> Handle(CreateProductCommand request, CancellationToken cancellationToken)
    {
        // Sanitiza os dados
        request.ProductData.Sanitize();
        
        // Valida o DTO
        if (!request.ProductData.IsValid())
        {
            var errors = request.ProductData.GetValidationErrors();
            return Result<Guid>.Failure(string.Join(", ", errors));
        }
        
        // Mapeia para entidade e persiste
        var product = _mapper.Map<Product>(request.ProductData);
        await _productRepository.AddAsync(product);
        
        return Result<Guid>.Success(product.Id);
    }
}
```

## 📈 Melhorias Implementadas

### Antes da Refatoração
- Validação básica via Data Annotations
- Apenas propriedades essenciais
- Sem sanitização automática
- Sem suporte a categorias/tags
- Sem validação de regras de negócio
- Sem suporte a dimensões físicas

### Após a Refatoração
- ✅ Validação robusta multicamada
- ✅ Sanitização automática de dados
- ✅ Suporte a categorias e tags
- ✅ Propriedades computadas para validação
- ✅ Suporte a dimensões físicas
- ✅ Metadados extensíveis
- ✅ Normalização para busca
- ✅ Factory methods para testing
- ✅ Coleta estruturada de erros
- ✅ Documentação XML completa
- ✅ Regras de negócio integradas

## 🛠️ Manutenção e Evolução

### Extensibilidade
- Metadados personalizados para necessidades específicas
- Validação extensível via métodos virtuais
- Suporte a novos campos via propriedades opcionais

### Performance
- Propriedades computadas lazy
- Validação eficiente com short-circuit
- Normalização otimizada

### Segurança
- Sanitização automática previne XSS
- Validação robusta previne injection
- Normalização consistente

## 📚 Casos de Uso

### Principais Cenários
1. **Criação via API**: Endpoint REST para criar produtos
2. **Import em Lote**: Validação de produtos importados
3. **Interface de Administração**: Formulários de criação
4. **Integração**: Recebimento de dados de sistemas externos
5. **Testes**: Criação de dados para testing

### Benefícios Alcançados
- **Integridade**: Dados sempre válidos e consistentes
- **Segurança**: Sanitização e validação robusta
- **Manutenibilidade**: Código limpo e testável
- **Flexibilidade**: Extensível via metadados
- **Performance**: Validação eficiente
- **Usabilidade**: Mensagens de erro claras

Esta implementação garante que todos os produtos sejam criados com dados válidos, consistentes e seguros desde o primeiro momento.
