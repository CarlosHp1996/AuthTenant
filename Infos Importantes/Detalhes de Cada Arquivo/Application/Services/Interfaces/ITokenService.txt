# ITokenService.cs - Documentação Técnica

## 📋 Visão Geral
A interface `ITokenService` define os contratos para serviços enterprise de geração, validação e manipulação de tokens JWT. Projetada para suportar operações seguras de autenticação em sistemas multi-tenant com padrões modernos de segurança e observabilidade.

## 🏗️ Arquitetura e Design

### Padrões Implementados
- **Interface Segregation Principle (ISP)**: Interface específica para operações de token
- **Dependency Inversion Principle (DIP)**: Abstração para inversão de dependências
- **Result Pattern**: Retorno consistente com controle de erro
- **Async/Await Pattern**: Operações assíncronas para melhor performance
- **Legacy Support Pattern**: Manutenção de compatibilidade retroativa

### Princípios SOLID
- **SRP**: Responsabilidade única para operações de token JWT
- **OCP**: Extensível para novos tipos de operações de token
- **LSP**: Contratos claros para implementações substituíveis
- **ISP**: Interface específica sem métodos desnecessários
- **DIP**: Abstração pura sem dependências concretas

## 🔧 Contratos de Métodos

### Métodos Principais (Modernos)

#### GenerateJwtTokenAsync
```csharp
Task<Result<string>> GenerateJwtTokenAsync(ApplicationUser user, CancellationToken cancellationToken = default);
```
**Propósito**: Gera token JWT seguro para autenticação  
**Entrada**: Usuário validado e token de cancelamento  
**Retorno**: Result contendo o token JWT ou erro  
**Exceções**: ArgumentNullException, InvalidOperationException

#### GetPrincipalFromExpiredTokenAsync
```csharp
Task<Result<ClaimsPrincipal>> GetPrincipalFromExpiredTokenAsync(string token, CancellationToken cancellationToken = default);
```
**Propósito**: Extrai claims de token expirado para refresh  
**Entrada**: Token JWT expirado  
**Retorno**: Result contendo ClaimsPrincipal ou erro  
**Exceções**: ArgumentNullException, SecurityTokenException

#### ValidateTokenAsync
```csharp
Task<Result<ClaimsPrincipal>> ValidateTokenAsync(string token, CancellationToken cancellationToken = default);
```
**Propósito**: Valida token JWT ativo com verificação completa  
**Entrada**: Token JWT para validação  
**Retorno**: Result contendo ClaimsPrincipal válido ou erro  
**Exceções**: ArgumentNullException, SecurityTokenValidationException

#### GenerateRefreshTokenAsync
```csharp
Task<Result<string>> GenerateRefreshTokenAsync(CancellationToken cancellationToken = default);
```
**Propósito**: Gera refresh token criptograficamente seguro  
**Entrada**: Token de cancelamento (opcional)  
**Retorno**: Result contendo refresh token ou erro  
**Segurança**: Usa RandomNumberGenerator para máxima entropia

### Métodos de Extração de Claims

#### GetTenantIdFromTokenAsync
```csharp
Task<Result<string>> GetTenantIdFromTokenAsync(string token, CancellationToken cancellationToken = default);
```
**Propósito**: Extrai ID do tenant para operações multi-tenant  
**Entrada**: Token JWT válido  
**Retorno**: Result contendo tenant ID ou erro  
**Multi-tenancy**: Suporte completo a isolamento de dados

#### GetUserIdFromTokenAsync
```csharp
Task<Result<string>> GetUserIdFromTokenAsync(string token, CancellationToken cancellationToken = default);
```
**Propósito**: Extrai ID do usuário do token  
**Entrada**: Token JWT válido  
**Retorno**: Result contendo user ID ou erro  
**Identificação**: Claim padrão NameIdentifier

#### IsTokenExpiredAsync
```csharp
Task<Result<bool>> IsTokenExpiredAsync(string token, CancellationToken cancellationToken = default);
```
**Propósito**: Verifica expiração sem validação completa  
**Entrada**: Token JWT para verificação  
**Retorno**: Result contendo status de expiração  
**Performance**: Verificação rápida sem validação custosa

### Métodos Legacy (Compatibilidade)

#### GenerateJwtToken (OBSOLETO)
```csharp
[Obsolete("Use GenerateJwtTokenAsync para melhor controle de erro e operações assíncronas.", false)]
string GenerateJwtToken(ApplicationUser user);
```
**Status**: Marcado como obsoleto, mantido para compatibilidade  
**Migração**: Usar GenerateJwtTokenAsync  
**Limitações**: Sem controle de erro, operação síncrona

#### GetPrincipalFromExpiredToken (OBSOLETO)
```csharp
[Obsolete("Use GetPrincipalFromExpiredTokenAsync para melhor controle de erro e operações assíncronas.", false)]
ClaimsPrincipal? GetPrincipalFromExpiredToken(string token);
```
**Status**: Marcado como obsoleto, mantido para compatibilidade  
**Migração**: Usar GetPrincipalFromExpiredTokenAsync  
**Limitações**: Sem controle de erro, retorno nullable

## 🔒 Aspectos de Segurança

### Validações de Entrada
- **Null Checks**: Validação rigorosa de parâmetros nulos
- **Token Format**: Verificação de formato JWT (3 partes)
- **Algorithm Validation**: Apenas algoritmos seguros (HMAC-SHA256)
- **Tenant Isolation**: Verificação de tenant em multi-tenancy

### Práticas de Segurança
- **Secure Claims**: Claims padronizados e seguros
- **Token Expiration**: Controle rigoroso de expiração
- **Algorithm Binding**: Vinculação a algoritmos específicos
- **Audit Logging**: Logs de segurança para auditoria

## 🎯 Casos de Uso por Cenário

### Authentication Flow
```csharp
// 1. Login - Geração de token
var tokenResult = await tokenService.GenerateJwtTokenAsync(user, cancellationToken);

// 2. Request Authorization - Validação
var principalResult = await tokenService.ValidateTokenAsync(token, cancellationToken);

// 3. Token Refresh - Uso de token expirado
var expiredPrincipalResult = await tokenService.GetPrincipalFromExpiredTokenAsync(expiredToken, cancellationToken);
```

### Multi-Tenant Operations
```csharp
// Extração de contexto tenant
var tenantResult = await tokenService.GetTenantIdFromTokenAsync(token, cancellationToken);

// Identificação de usuário
var userResult = await tokenService.GetUserIdFromTokenAsync(token, cancellationToken);
```

### Token Management
```csharp
// Verificação de expiração
var expiredResult = await tokenService.IsTokenExpiredAsync(token, cancellationToken);

// Geração de refresh token
var refreshResult = await tokenService.GenerateRefreshTokenAsync(cancellationToken);
```

## 📊 Result Pattern Implementation

### Estrutura de Retorno
```csharp
public sealed class Result<T>
{
    public bool IsSuccess { get; }
    public T? Data { get; }
    public string? Error { get; }
    public List<string> Errors { get; }
}
```

### Tratamento de Erros
```csharp
var result = await tokenService.GenerateJwtTokenAsync(user);

if (result.IsSuccess)
{
    var token = result.Data; // Token gerado com sucesso
}
else
{
    var error = result.Error; // Mensagem de erro específica
    // Log do erro e tratamento apropriado
}
```

## 🧪 Estratégias de Teste

### Unit Tests Essenciais
```csharp
[Test]
public async Task GenerateJwtTokenAsync_ValidUser_ReturnsSuccessResult()
{
    // Arrange: User válido
    // Act: Gerar token
    // Assert: Result.IsSuccess = true
}

[Test]
public async Task ValidateTokenAsync_ExpiredToken_ReturnsFailureResult()
{
    // Arrange: Token expirado
    // Act: Validar token
    // Assert: Result.IsSuccess = false, Error = "Token has expired"
}
```

### Integration Tests
```csharp
[Test]
public async Task TokenLifecycle_GenerateValidateRefresh_WorksCorrectly()
{
    // Teste completo do ciclo de vida do token
}
```

### Mock Implementation
```csharp
public class MockTokenService : ITokenService
{
    // Implementação mock para testes
    // Retorna resultados predefinidos para cenários específicos
}
```

## 📈 Performance Considerations

### Async Operations
- **Non-blocking**: Todas as operações são assíncronas
- **Cancellation Support**: Suporte completo a CancellationToken
- **Resource Efficiency**: Operações não bloqueiam threads

### Caching Strategy
- **Configuration Cache**: Cache das configurações JWT
- **Token Validation Cache**: Cache de validações recentes (implementação específica)
- **Claims Cache**: Cache de claims extraídos (opcional)

## 🔄 Migration Path

### De Métodos Legacy para Modernos
```csharp
// Antes (Legacy)
try 
{
    var token = tokenService.GenerateJwtToken(user);
    // Usar token
}
catch (Exception ex)
{
    // Tratamento de exceção
}

// Depois (Moderno)
var result = await tokenService.GenerateJwtTokenAsync(user, cancellationToken);
if (result.IsSuccess)
{
    var token = result.Data;
    // Usar token
}
else
{
    // Tratamento de erro específico: result.Error
}
```

### Benefícios da Migração
- ✅ **Controle de Erro**: Result pattern ao invés de exceções
- ✅ **Operações Assíncronas**: Melhor performance e escalabilidade
- ✅ **Cancellation Support**: Possibilidade de cancelar operações
- ✅ **Type Safety**: Retornos tipados e seguros
- ✅ **Testabilidade**: Mais fácil de testar e mockar

## 📚 Melhorias Implementadas

### Antes da Refatoração
- Interface simples com 2 métodos
- Sem controle de erro estruturado
- Operações síncronas apenas
- Sem suporte a multi-tenancy avançado
- Sem métodos de utilidade

### Após a Refatoração
- ✅ Interface enterprise completa com 8 métodos
- ✅ Result pattern para controle consistente de erro
- ✅ Operações assíncronas com CancellationToken
- ✅ Suporte completo a multi-tenancy
- ✅ Métodos de utilidade para extração de claims
- ✅ Compatibilidade retroativa com métodos legacy
- ✅ Documentação XML completa
- ✅ Marcação de obsolescência para migração gradual
- ✅ Suporte a refresh tokens
- ✅ Validação avançada de tokens

Esta interface representa um contrato enterprise robusto para operações de token JWT, seguindo as melhores práticas de segurança, performance e manutenibilidade.
